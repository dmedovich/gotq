// This Docusaurus site is a separate codeless module so website assets and the
// npm lockfile are excluded from the gotq module zip downloaded by Go users.
// There is no Go package here; npm builds the site.
module github.com/dmedovich/gotq/website

go 1.23
